/**
 * Invariant companion for @deepseek-ai/dsh-hello.
 * No runtime invariant; every contribution is registry-disposed.
 * @module @deepseek-ai/dsh-hello/invariant
 */

import type {} from '@deepseek-ai/dsh-invariants'

/** No runtime invariant for the hello bundle. */
export const message = 'hello bundle contributions are registry-disposed'
